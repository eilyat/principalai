---
import { getCollection, type CollectionEntry, getEntry } from "astro:content";
import PublicationsLayout from "@/layouts/IndustriesLayout.astro";

import "@/styles/global.css";

export async function getStaticPaths() {
  const publications = await getCollection("capabilities");
  return publications.map((entry) => ({
    params: { slug: entry.slug },
    props: { entry },
  }));
}

type Props = {
  entry: CollectionEntry<"capabilities">;
};

const { entry } = Astro.props;
const { Content } = await entry.render();

---

<PublicationsLayout frontmatter={entry.data}>
  <Content />
</PublicationsLayout>